name: sidecar-ci

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  release-readiness:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install WXT dependencies
        working-directory: extensions/wxt
        run: bun install --frozen-lockfile

      - name: Check WXT
        working-directory: extensions/wxt
        run: bun run check

      - name: Package WXT release artifacts
        working-directory: extensions/wxt
        run: bun run package:release

      - name: Install legacy parity dependencies
        working-directory: extensions/sidecar
        run: bun install --frozen-lockfile

      - name: Install Playwright Chromium
        working-directory: extensions/sidecar
        run: bunx playwright install --with-deps chromium

      - name: Run legacy unit tests
        working-directory: extensions/sidecar
        run: bun run test:unit

      - name: Run WXT parity browser tests
        working-directory: extensions/sidecar
        run: bunx playwright test tests/e2e/wxt-parity.spec.ts

      - name: Setup Node for website
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install website dependencies
        working-directory: web
        run: npm ci

      - name: Check website
        working-directory: web
        run: npm run check

      - name: Build website
        working-directory: web
        run: npm run build

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: sidecar-release-artifacts
          path: |
            extensions/wxt/release/manifest.json
            extensions/wxt/release/SHA256SUMS.txt
            extensions/wxt/release/artifacts/*.zip
