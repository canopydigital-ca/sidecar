# Sidecar UI

Facade exports for Sidecar's shared Svelte UI.

- WXT should import UI helpers and components through this package surface.
- Implementation currently delegates to the shared runtime copy while the migration settles.
